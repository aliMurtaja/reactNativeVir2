import React, {useState, useEffect} from 'react';
import { StyleSheet, Text, View, TextInput, Image, Button, AsyncStorage } from 'react-native';

import AllScreens from './../Main/Pages';

export default (props) =>{
    console.log(AllScreens)
    return (
        <AllScreens /> 
    )
    
}